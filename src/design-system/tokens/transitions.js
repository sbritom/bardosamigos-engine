import { animations } from './animations.js'

export const transitions = Object.freeze({
  hover: animations.patterns.hover,
  fade: animations.patterns.fade,
  slide: animations.patterns.slide,
  focus: animations.patterns.focus,
  loading: animations.patterns.loading,
  skeleton: animations.patterns.skeleton,
})
