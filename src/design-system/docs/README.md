# Bar Design System

Documentacao interna da biblioteca oficial de UI do Bar dos Amigos Engine.

Entrada principal:

- `docs/BAR_DESIGN_SYSTEM.md`

Regras principais:

- Reutilizar tokens de `src/design-system/tokens`.
- Reutilizar componentes de `src/design-system/components`.
- Usar `lucide-react` como pacote oficial de icones.
- Evitar estilos isolados em novas telas.
- Aplicar o BDS gradualmente para nao alterar telas existentes sem sprint visual dedicada.
